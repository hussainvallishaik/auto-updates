{
  "name": "electron-auto-update",
  "version": "1.0.0",
  "description": "auto update for windows",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hussainvallishaik/auto-updates.git"
  },
  "author": "hussain valli",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.0.1",
    "electron-builder": "^22.3.2"
  }
}